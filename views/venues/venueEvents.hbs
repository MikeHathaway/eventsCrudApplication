<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
      {{#each venue}}
        <h1>{{this.name}}</h1>
        <p>{{this.email}}</p>
        <ul>
          <li>{{this.title}}</li>
        </ul>
        <p>{{this.start_datetime}}</p>
        <a href="../events/{{this.id}}"><p>Get More Information!</p></a>
      {{/each}}
  </body>
</html>
